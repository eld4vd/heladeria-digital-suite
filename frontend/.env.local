# ========================================
# VARIABLES DE ENTORNO - DESARROLLO LOCAL
# Utiliza estas variables cuando ejecutes `npm run dev` o el stack Vite fuera de Docker.
# ========================================

# URL del backend NestJS corriendo en local
VITE_BASE_URL_ENDPOINT=http://localhost:3000/api

# Endpoint para obtener el token CSRF
VITE_CSRF_ENDPOINT=/auth/csrf

# Nombre de la cookie que expone el backend para CSRF (legible por el navegador)
VITE_CSRF_COOKIE_NAME=csrf_token
